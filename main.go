package main

func main() {

	//config, _ := ReadPropertiesFile("Resources/application.properties")
	//customerDebit(config)

	//dbUrl := fmt.Sprintf("host=%s user=%s dbanme=%s sslmode=disable passowrd=%s port=%s", config["host"], config["username"], config["dbname"], config["password"], config["db_port"])
	//dialect := config["dialect"]
	//db, err = gorm.Open(dialect, dbUrl)

	printStarPatterns(5)
}
